{
  "name": "backend-boilerplate",
  "version": "0.0.1",
  "scripts": {
    "build": "cd app && bundle update --bundler && bundle update && npm install",
    "start": "cd app && bin/rails server",
    "test": "cypress run"
  },
  "devDependencies": {
    "cypress": "^5.4.0"
  }
}
